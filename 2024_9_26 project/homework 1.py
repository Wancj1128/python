# 请编写一个py程序，找出200以内（包括200）的所有回文数，
# 并以列表的形式输出（比如，121从左往右读与从左往右读是一样的，这种数被称为回文数）
# 要求如下：
# 自定义一个函数，命名为huiwen，用于判断一个数是否为回文数。
# 在主程序中，使用循环语句遍历200以内的所有数字，并调用huiwen函数来判断每个数字是否为回文数。
# 将所有回文数收集到一个列表中，并在程序结束时打印该列表
import math
def huiwen(s):
